M.FIND(1)            User Manuals M.FIND(1)            M.FIND(1)

NAME
    m.find - walks a file hierarchy.

DESCRIPTION
    the m.find utility recursively descends the
     directory tree for each path listed, evalu-
     ating an expression in terms of each file in the tree.

SYNOPSIS
    m.find [options] [...directories]

OPTIONS
    -h, --help
      outputs usage information.

    -v, --version
      outputs version number.s

    -H, --hidden
      list hidden files and walk hidden folders.

    -f, --filter "pattern"
      true if the last component of the pathname being examined
      matches the pattern. the match is case insensitive.

    -i, --ignore "pattern"
      false if the last component of the pathname being examined
      matches the pattern. the match is case insensitive.

M.FIND(1)      https://github.com/ivoputzer/m.find     M.FIND(1)
